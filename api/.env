# PostgreSQL connection settings
DB_HOST=localhost
DB_NAME=mydatabase
DB_USER=myuser
DB_PASSWORD=mypassword
# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://ui.nagp.com
# Optional: You can add a custom port if needed
# PORT=5000
